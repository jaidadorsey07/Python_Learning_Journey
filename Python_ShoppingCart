# Shopping Cart Program 

# This program will contain two lists:
# not using tuples because they are unchangable 
foods = []
prices = []
total = 0 

while True: 
    food = input("Enter a food to buy (q to quit): ")
    if food.lower() == "q": 
        break # if the statement is true, you will need to implement a way to break out of the loop 
    else: 
        price = float(input(f"enter the price of a {food}: $"))
        foods.append(food) # this will add all the entered foods to the intialized list "foods"
        prices.append(price) # this will add all the entered foods to the intialized list 

print("----- YOUR CART -----")        

for food in foods: # printing each food iteration
    print(food)
    
for price in prices: # adding up each print iteration
    total += price # remember (+=) is the same as total = price + total 
 
print(f"Your total is: ${total}")