# format specifiers = {value:flags} format a value based on what flags are inserted 

# .(number)f = round to that many decimal places (fixed point)
# :number = allocate that many spaces 
# :03 = allocate and zero pad that many spaces 
# :< = left justify 
# :> = right justify 
# :^ = center align 
# :+ = use a plus sign to indicate positive value 
# : = = place sign to leftmost position
# : = insert a space before positive numbers 
# :, = comma separator 

price1 = 3000.14159
price2 = -9870.65
price3 = 1200.34 

print(f"Price 1 is ${price1:.2f}")   # when you add a colon, you can add a flag which will format 
print(f"Price 2 is ${price2:.2f}")   # our value a particular way
print(f"Price 3 is ${price3:.2f}")
# using :.2f rounds the values to 2 decimal points 

# To allocate space to display a value.. add a number after a colon
print(f"Price 3 is ${price3:10}")    # there are now 10 total spaces 

# to zero pad within those spaces add a 0 before the value for spaces 
print(f"Price 3 is ${price3:010}")

# to left justify a value you would use (<)
print(f"Price 3 is ${price3:<10}")
# to right justify a value you would use (>)
print(f"Price 3 is ${price3:>10}")
# to center allign a value you would use (^)
print(f"Price 3 is ${price3:^10}")

# if you want to dispay a plus sign for positive numbers you use a plus
# the plus will not show on negative values 
print(f"Price 1 is ${price1:+}")   
print(f"Price 2 is ${price2:+}")   
print(f"Price 3 is ${price3:+}")

# thousands separator 
print(f"Price 1 is ${price1:,.2f}")  # ',' adds a comma for values in the thousand 
print(f"Price 2 is ${price2:,.2f}")   
print(f"Price 3 is ${price3:,.2f}")  # you can add multiple variations of flags like :+,.2f